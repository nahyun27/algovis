import type { Algorithm } from '../../types';

export const TSP: Algorithm = {
  id: 'tsp',
  name: 'Traveling Salesperson Problem',
  slug: 'tsp',
  description: 'Find the shortest possible route that visits every city exactly once and returns to the origin city.',
  difficulty: 'Hard',
};

// Further details on the simulation nodes, logic steps, 
// and specific solver structures will be elaborated later.
